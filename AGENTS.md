# AGENTS.md

本文件为在本仓库中工作的 AI 编码代理提供项目约定和操作指南。

## 项目概览

- 这是一个轻量静态网站项目。
- 当前主要入口是 `index.html`。
- 目前没有发现构建工具、包管理器配置或测试框架。

## 工作目录

- 仓库根目录：`C:\Users\IMPACT\.codex\worktrees\ea8f\vercel_test_website`
- 站点入口：`index.html`

## 运行与预览

- 这个项目可以直接在浏览器中打开 `index.html` 预览。
- 如需通过本地服务验证，可使用任意静态文件服务器，例如：

```powershell
python -m http.server 3000
```

然后访问 `http://localhost:3000`。

## 编辑规范

- 保持项目简单，优先使用原生 HTML、CSS 和 JavaScript。
- 不要引入新的框架、构建工具或依赖，除非用户明确要求。
- 修改页面时优先保持现有视觉风格和文件结构。
- 避免无关重构，只改动与当前任务直接相关的内容。
- 注意中文内容的编码，保存文件时使用 UTF-8。

## 验证建议

完成页面变更后，至少检查：

- 页面能正常打开，没有明显 HTML 语法错误。
- 桌面和移动宽度下布局不重叠、不溢出。
- 中文文本显示正常，没有乱码。
- 交互元素在浏览器中可用。

## Git 注意事项

- 不要还原用户未明确要求还原的改动。
- 提交前先查看 `git status`，确认只包含本次任务相关文件。
- 若工作区已有无关改动，保留它们并仅处理当前任务范围。
